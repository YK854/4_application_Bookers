<h2>New book</h2>
<!--model注意-->
<%= form_with model: Book.new, url:"/books", local:true do |b| %>
  <h4>Title</h4>
  <%= b.text_field :title %>
  <h4>Body</h4>
  <%= b.text_area :body %>
  <%= b.submit 'Create Book' %>
<% end %>